// import ProductsManager from "../src/DAO/productsDAO.js"
// import Assert from "assert"

// const assert = Assert.strict 

// describe("Testing Products Dao", () => {
//     before(function () {
//         this.productDao = new ProductsManager()
//     })

//     it("El Dao debe poder obtener todos los productos de la DDBB", async function() {
//         // Establece un límite de tiempo de espera más largo (por ejemplo, 20 segundos)
//         this.timeout(20000);

//         console.log(this.productDao);

//         const result = await this.productDao.getAllProducts();
//         assert.strictEqual(Array.isArray(result), true);
//     })
// })
