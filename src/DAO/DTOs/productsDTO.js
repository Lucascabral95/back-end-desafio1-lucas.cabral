export class ProductsFsDTO {

    constructor (id, title, description, code, price, stock, category, status){
     this.id = id 
     this.title = title 
     this.description = description 
     this.code = code
     this.price = price
     this.stock = stock
     this.category = category
     this.status = status
    }
}