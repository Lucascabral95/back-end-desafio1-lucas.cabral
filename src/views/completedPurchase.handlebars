<div class="container-fluid">

    <div class="header-mongodb">
        <div>
            <a href="/api/session/logout" class="btn btn-primary" id="log">LOGOUT</a>
            <a href="/home-mongodb" class="btn btn-warning bc">Ir a la seccion de productos</a>
            <a href="/api/users/premium" class="btn btn-danger bc">Cambiar rol de usuario</a>
            <a href="/api/users/documents" class="btn btn-info bc">Cambiar foto de perfil</a>
        </div>
        <div style="display: flex">
            <div style="display: flex; align-items:center">
                <p style="font-size: 1.4rem;
                            color: blue;
                            font-weight: bolder;
                            position: relative;
                            right: 10%;" class="texto-perfil"> Usuario: {{mock.[8]}} </p>
            </div>
            {{#if mock.[7] }}
            <a href="api/users/documents">
                <img class="cuadro-foto-perfil" src="{{mock.[6]}}" alt="Foto de perfil">
            </a>
            {{else}}
            <a href="api/users/documents">
                <img class="cuadro-foto-perfil" src="/img/foto-anonima.jpg" alt="Foto de perfil">
            </a>
            {{/if}}
        </div>
    </div>

    <div class="container">
        <a href="/home-mongodb" class="btn btn-primary">Volver a la sección de productos</a>

        <h2 class="title-compra-realizada">¡¡¡Felicidades por terminar tu compra!!!</h2>

        <h5 class="subtitle-compra-realizada">Tu ultimo ticket de compra se encuentra acá abajo 👇 </h5>
        <h5 style="color: red; font-size: 1.4rem" class="subtitle-compra-realizada">*A su vez, se desconto el stock de
            los
            productos en la base de datos y se vacío el carrito*</h5>

        <div class="col-12">
            <table style="margin-top: 3.5%;" class="table table-striped col-12">
                <thead>
                    <tr>
                        <th scope="col">_id</th>
                        <th scope="col">Code</th>
                        <th scope="col">Purchase datatime</th>
                        <th scope="col">Amount</th>
                        <th scope="col">Email del comprador</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td> {{ticket._id}} </td>
                        <td> {{ticket.code}} </td>
                        <td> {{ticket.purchase_dateTime}} </td>
                        <td> {{ticket.amount}} </td>
                        <td> {{ticket.purchaser}} </td>
                    </tr>
                </tbody>
            </table>

            <h2
                style="color: blue; font-weight: bolder; text-align: center; font-size: 2rem; margin-top: 7%; margin-bottom: 2%">
                Todos los Tickets generados por este Carrito ({{mock.[5]}} en total) </h2>

            <table style="margin-top: 3.5%;" class="table table-striped col-12">
                <thead>
                    <tr>
                        <th>_id</th>
                        <th>Code</th>
                        <th>Purchase datatime</th>
                        <th>Amount</th>
                        <th>Email del comprador</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each mock.[0] as |ticketId index|}}
                    <tr>
                        <td scope="col">{{ticketId}}</td>
                        <td scope="col">{{lookup ../mock.[3] index}}</td>
                        <td scope="col">{{lookup ../mock.[1] index}}</td>
                        <td scope="col">{{lookup ../mock.[4] index}}</td>
                        <td scope="col">{{lookup ../mock.[2] index}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>

        </div>
    </div>

</div>